describe("Account tests", () => {
    let account: Account

    // beforeEach(() => {
        
    // })

    it("should add enemy to the game", () => {
        expect(account.accountNum).toEqual("12345")
    })

})